from setuptools import setup

setup(
    name="mili",
    version="0.9",
    description="Minimal immediate-mode python user interface library",
    author="Damus",
    packages=["mili"],
    install_requires=["pygame-ce"],
)
